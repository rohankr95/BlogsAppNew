import React from 'react';

const Navbar = () => {
  return <>
     <nav class="navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Relevel Blogs</a>
        </div>
    </nav>
  </>;
};

export default Navbar;
